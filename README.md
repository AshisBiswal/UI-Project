Data on CO2 emissions and Carbon footprint

Carbon foorprint and co2 emisions are the key metrics 
calculated on yearly basis data.

Source data and code:

The charts are built using the chart.js javascript library from the data thats has been
 collected, aggregrated stored in
Firebase.
The User Interface  is built with HTML5 , enriched with CSS styles.
